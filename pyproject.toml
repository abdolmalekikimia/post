[project]
name = "sorting-device-postal-integration-tests"
version = "0.1.0"
description = "Python tests for sorting device and postal system integration"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
